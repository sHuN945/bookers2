<h1>New book</h1>
  <%= form_with model: @book do |f| %>
    <h4>画像</h4>
      <%= f.file_field :image, accept: "image/*" %>
    <h4>Book</h4>
      <%= f.text_field :book_name %>
    <h4>Caption</h4>
      <%= f.text_area :caption %>
    <%= f.submit '投稿' %>
  <% end %>